<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Fake Merchant Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <h1 class="text-center mb-4">Merchant Admin Dashboard</h1>

    <div class="card mb-4">
        <div class="card-header">Recent Charges</div>
        <div class="card-body">
            {% for charge in charges %}
                <p><b>Card:</b> {{ charge.pan }} | <b>Balance:</b> {{ charge.balance }} | <b>Status:</b> {{ charge.status }}</p>
            {% endfor %}
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header">Captured Batches</div>
        <div class="card-body">
            {% for batch in batches %}
                <p><b>Batch ID:</b> {{ batch.batch_id }} | <b>Total:</b> ${{ batch.total_amount }} | <b>Date:</b> {{ batch.timestamp }}</p>
            {% endfor %}
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header">ACH Settlements</div>
        <div class="card-body">
            {% for settlement in settlements %}
                <p><b>Settlement ID:</b> {{ settlement.settlement_id }} | <b>Amount:</b> ${{ settlement.amount }} | <b>Date:</b> {{ settlement.pushback_timestamp }}</p>
            {% endfor %}
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header">Wire Transfers</div>
        <div class="card-body">
            {% for wire in wires %}
                <p><b>Wire ID:</b> {{ wire.wire_transfer_id }} | <b>Amount:</b> ${{ wire.amount }} | <b>Reference:</b> {{ wire.reference_number }}</p>
            {% endfor %}
        </div>
    </div>
</div>

</body>
</html>
